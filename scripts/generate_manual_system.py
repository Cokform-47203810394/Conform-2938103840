from __future__ import annotations

from pathlib import Path
import shutil

ROOT = Path(__file__).resolve().parents[1]
OUT = ROOT / "docs" / "operations-manuals"

DOMAINS = [
    ("GOV", "운영 원칙·의사결정", "창업가", "제품 방향, 우선순위, 중단 기준을 관리한다.", ["주간 운영 점검", "월간 목표 설정", "기능 유지 판단", "기능 중단 판단", "의사결정 기록", "우선순위 재정렬", "리스크 등록", "회의 최소화", "외부 요청 판정", "운영 문서 갱신"]),
    ("PRD", "제품 발견·기획", "기획가", "사용자 문제와 가설을 검증 가능한 제품 결정으로 바꾼다.", ["문제 인터뷰 준비", "인터뷰 진행", "가설 작성", "작업 흐름 정의", "성공 기준 설정", "기능 요구 작성", "범위 축소", "사용자 시나리오 점검", "기능 제거 판단", "검증 결과 정리"]),
    ("UXD", "UX·접근성·콘텐츠", "디자이너", "사용자가 빠르게 이해하고 실수 없이 작업하도록 화면과 문구를 설계한다.", ["정보 위계 점검", "카드 사용 판단", "타이포그래피 점검", "모바일 흐름 점검", "접근성 점검", "빈 상태 작성", "오류 문구 작성", "폼 작성 흐름 점검", "응답자 흐름 점검", "디자인 회귀 점검"]),
    ("FEN", "프런트엔드", "프런트엔드 개발자", "React 화면, 상태, 라우팅, 상호작용을 안전하게 변경한다.", ["화면 변경 준비", "컴포넌트 분리", "상태 변경", "라우팅 변경", "폼 입력 변경", "모바일 회귀 점검", "오류 경계 점검", "접근성 속성 점검", "성능 점검", "프런트 배포 점검"]),
    ("BEN", "백엔드·데이터", "백엔드 개발자", "Supabase 데이터 모델과 서버 경계를 안전하게 운영한다.", ["스키마 변경", "RLS 정책 점검", "RPC 변경", "Edge Function 변경", "마이그레이션 적용", "데이터 조회 점검", "데이터 삭제 작업", "알림 데이터 처리", "성능 병목 점검", "서버 장애 복구"]),
    ("SEC", "보안·암호화·키", "보안 담당자", "E2EE 경계와 개인키·복구수단을 사용자 소유로 유지한다.", ["키 생성 점검", "키 백업 생성", "키 복구 테스트", "기기 교체 안내", "키 불일치 대응", "취약점 제보 처리", "보안 사건 분류", "비밀값 교체", "권한 최소화 점검", "암호화 회귀 점검"]),
    ("QAT", "품질·테스트", "QA 엔지니어", "핵심 사용자 경로와 회귀 위험을 배포 전에 확인한다.", ["스모크 테스트", "응답 제출 테스트", "폼 생성 테스트", "복구 흐름 테스트", "모바일 테스트", "브라우저 호환성", "오류 재현", "회귀 테스트 추가", "릴리스 승인", "버그 종료 판정"]),
    ("OPS", "배포·운영·장애", "SRE·운영 담당자", "Cloudflare Pages, GitHub Actions, Supabase의 정상 상태를 유지한다.", ["배포 전 점검", "배포 상태 확인", "배포 실패 대응", "운영 장애 분류", "롤백 판단", "상태 페이지 공지", "일일 운영 점검", "주간 운영 점검", "의존 서비스 장애 대응", "사후 회고"]),
    ("DAT", "데이터 수명·백업", "데이터 운영 담당자", "암호문 응답의 보관·파기·백업과 내보내기 책임을 관리한다.", ["응답 보관 기간 설정", "파기 작업 확인", "암호화 백업 실행", "백업 복구 점검", "내보내기 점검", "삭제 요청 처리", "데이터 최소화 점검", "복구 번들 점검", "보관 정책 변경", "데이터 사고 격리"]),
    ("SUP", "고객지원", "고객지원 담당자", "비밀정보를 받지 않고 사용자가 스스로 문제를 해결하게 돕는다.", ["문의 분류", "로그인 문제 안내", "공개 링크 문제 안내", "응답 미수신 안내", "키 복구 안내", "내보내기 안내", "버그 제보 수집", "환불·결제 문의 분리", "악용 신고 처리", "문의 종료 기록"]),
    ("LEG", "개인정보·컴플라이언스", "개인정보 운영 담당자", "필요 최소한의 정보만 수집하고 정책·고지·처리 책임을 분명히 한다.", ["수집 목적 검토", "개인정보 고지 설정", "동의 문항 검토", "보유 기간 검토", "국외 처리 고지", "위탁 여부 검토", "정보주체 요청 접수", "정책 변경 고지", "컨시어지 테스트 검토", "법률 검토 요청 준비"]),
    ("FIN", "비용·벤더", "재무·벤더 담당자", "무료 한도와 고정비를 지키면서 서비스 의존성을 통제한다.", ["월간 비용 점검", "무료 한도 점검", "벤더 변경 검토", "도메인 비용 점검", "API 비용 점검", "계약 갱신 확인", "결제 수단 점검", "비용 이상 탐지", "예산 승인", "비용 기록 정리"]),
    ("MRK", "브랜드·마케팅", "마케터", "한국 실무형 폼 서비스의 신뢰와 사용 장면을 일관되게 전달한다.", ["브랜드 메시지 점검", "사례 작성", "랜딩 카피 점검", "공유 이미지 점검", "채널 게시물 작성", "파일럿 모집", "후기 요청", "캠페인 회고", "경쟁 메시지 점검", "브랜드 자산 사용 점검"]),
    ("SEO", "콘텐츠·검색", "콘텐츠·SEO 담당자", "사용자의 문제 검색을 실질적인 가이드와 템플릿 진입으로 연결한다.", ["검색 의도 정의", "가이드 작성", "템플릿 설명 작성", "제목·설명 점검", "사이트맵 점검", "OG 정보 점검", "내부 링크 점검", "사례 페이지 작성", "콘텐츠 갱신", "검색 성과 검토"]),
    ("GRW", "분석·성장", "그로스 담당자", "응답 원문 없이 유입부터 재사용까지의 제품 행동을 측정한다.", ["이벤트 설계", "개인정보 제외 점검", "UTM 규칙 설정", "유입 분석", "활성화 분석", "재방문 분석", "퍼널 이탈 분석", "실험 설계", "실험 종료", "월간 지표 회고"]),
    ("COM", "커뮤니티·파트너", "커뮤니티 담당자", "Discord에 의존하지 않고 한국 실무 운영자와 상호 신뢰를 만든다.", ["커뮤니티 탐색", "참여 규칙 확인", "첫 연락 작성", "파일럿 제안", "파트너 미팅", "피드백 수집", "커뮤니티 게시", "이슈 대응", "관계 종료", "파트너 성과 회고"]),
    ("SAL", "컨시어지·고객개발", "창업가·운영자", "원문 개인정보를 받지 않고 실제 업무 흐름을 관찰해 제품 가설을 검증한다.", ["대상 선정", "안내문 발송", "더미 데이터 세팅", "폼 구조 지원", "테스트 동행", "인터뷰 진행", "요청 범위 통제", "민감정보 차단", "재사용 요청", "테스트 종료"]),
    ("PEO", "협업자·외주", "운영 책임자", "협업자에게 최소 권한과 명확한 산출물 기준을 제공한다.", ["협업자 온보딩", "권한 부여", "작업 브리프 작성", "코드 리뷰", "디자인 검수", "외주 산출물 수령", "접근 권한 회수", "비밀유지 확인", "역할 변경", "협업 종료"]),
    ("RES", "시장·경쟁 리서치", "리서처", "경쟁 기능이 아니라 사용자 문제와 대체 수단을 기준으로 시장을 해석한다.", ["경쟁사 선정", "기능 비교", "가격 비교", "사용자 리뷰 분석", "대체 수단 분석", "시장 가설 작성", "리서치 출처 확인", "인사이트 정리", "결정 연결", "리서치 폐기"]),
    ("BCP", "연속성·위기대응", "대표·운영 책임자", "서비스·데이터·커뮤니케이션의 중단 상황에서 피해를 제한하고 복구한다.", ["사고 감지", "초기 격리", "영향 범위 파악", "사용자 공지", "복구 의사결정", "백업 복원", "계정 탈취 대응", "데이터 유출 의심 대응", "서비스 종료 준비", "사후 회고"]),
]

CONTEXTS = [
    "정기 점검", "신규 요청", "실사용자 신고", "배포 직전", "배포 직후",
    "장애 발생", "보안 의심", "개인정보 포함 가능성", "파일럿 진행", "월간 회고",
]

CORE_PLAYBOOKS = """# 핵심 플레이북: 이 문서만 먼저 읽기

> 이 문서는 2,000개 세부 매뉴얼보다 먼저 읽는 운영 기준이다. 비밀값·개인키·응답 원문은 어떤 지원·검증·마케팅 과정에서도 받거나 기록하지 않는다.

## 1. 기능 추가를 멈추는 기준

사용자 관찰, 사용 기록, 재사용 의사가 없는 기능 요청은 백로그에만 기록하고 구현하지 않는다. 현재 콕폼은 검증 동결 상태이므로 테스트·복구·접근성·개인정보 최소화·측정 이외의 변경은 대표가 이유와 성공 기준을 문서화할 때만 재개한다.

## 2. 배포 절차

변경 전에는 `git diff --check`와 `npm run build`를 실행한다. 배포 후에는 GitHub Actions 성공 여부, 홈·폼 편집기·공개 응답 링크의 스모크 확인, 브라우저 콘솔의 새 렌더링 오류 여부를 확인한다. 실패 시 새 수정으로 덮지 말고 마지막 정상 커밋을 기준으로 원인을 분리한다.

## 3. E2EE와 복구

응답 원문은 개인키를 가진 작성자만 연다. 비밀번호를 잊어도 암호문은 보관 기간까지 남아야 하지만, 서비스가 비밀번호 재설정으로 열어주면 안 된다. 복구는 키 백업 파일·전체 복구 번들·기존 로그인 기기에서만 가능해야 한다. 개인키, 복구 비밀번호, 키 백업 파일을 지원 채널로 받지 않는다.

## 4. 응답 제한의 정직한 표현

브라우저 1회 제한은 새 브라우저 프로필과 시크릿 창에서 바뀔 수 있다. 채용·투표·경품처럼 공정성 또는 개인 식별이 필요한 폼에 ‘한 사람 1회’를 보장한다고 쓰지 않는다. 필요하면 로그인 기반, 초대 토큰, 운영자 승인 중 하나를 명시적으로 선택한다.

## 5. 컨시어지 테스트

콕폼 운영자는 실제 신청자 원문, 이력서, 연락처, 민감정보를 대신 받거나 세팅하지 않는다. 운영자의 질문 구조는 더미 데이터로 받고, 응답 원문은 그 운영자가 소유하는 폼에서만 처리한다. 첫 테스트에서는 채용·의료·금융·미성년자·민감정보 수집 폼을 제외한다.

## 6. 개인정보 없는 측정

측정 가능한 항목은 유입 출처, 템플릿 열람, 폼 생성, 첫 질문 추가, 공유 링크 생성, 재방문, 내보내기, 오류 경계 도달뿐이다. 이메일, 답변 원문, 개인키, 복구 수단은 이벤트에 넣지 않는다.

## 7. 고객지원 원칙

고객에게 복구 비밀번호·개인키·키 백업·응답 원문을 보내달라고 요청하지 않는다. 문제를 재현할 때는 폼 ID, 시각, 브라우저 버전, 오류 코드, 민감정보가 제거된 화면만 받는다.

## 8. 장애 대응 원칙

공개 응답 제출·응답 복호화·로그인 중 하나가 실패하면 먼저 새 배포를 멈추고 영향을 확인한다. 원인을 모르는 상태에서 DB, 키, 암호문을 수정하지 않는다. 사용자에게는 사실·영향·다음 확인 시각만 알리고 추측을 공지하지 않는다.

## 9. 완료 정의

구현 완료, 배포 완료, 안전 완료, 검증 완료는 다르다. 사용자가 실제 업무에 다시 사용했거나 명확한 거절 이유가 기록되기 전에는 기능이 성공한 것이 아니다.

## 10. 매뉴얼 사용법

`INDEX.csv`에서 역할·도메인·업무·상황을 찾아 해당 ID의 파일을 연다. 세부 파일은 체크리스트이며, 실제 판단 기준은 이 핵심 플레이북과 도메인별 README를 우선한다.
"""

README = """# Cokform 독립 운영 매뉴얼 시스템

이 디렉터리는 콕폼을 혼자서도 운영·개발·보안 관리할 수 있도록 만든 **2,000개 세부 실행 매뉴얼**과 핵심 플레이북이다.

| 구성 | 수량 | 용도 |
|---|---:|---|
| 도메인 | 20개 | 역할과 책임 경계 |
| 업무 흐름 | 도메인당 10개 | 실제 반복 업무 |
| 상황 | 업무 흐름당 10개 | 정기·요청·배포·장애·개인정보 등 맥락 |
| 세부 매뉴얼 | **2,000개** | 하나의 상황에서 바로 실행하는 체크리스트 |

## 먼저 읽을 파일

1. [`00-core-playbooks.md`](./00-core-playbooks.md): 절대 원칙과 금지 사항
2. [`INDEX.csv`](./INDEX.csv): 2,000개 항목 검색·필터용 목록
3. [`DOMAIN-README.md`](./DOMAIN-README.md): 도메인별 책임과 기존 문서 연결
4. 원하는 `manuals/<도메인>/<ID>.md`: 상황별 실행 절차

## 공통 판단 순서

1. 이 일이 사용자 답변·개인키·복구수단을 보게 하는가? 그러면 멈추고 보안·개인정보 매뉴얼을 먼저 연다.
2. 이 일이 새 기능인가? 검증 동결 상태라면 구현하지 않고 가설·성공 기준부터 기록한다.
3. 이 일이 배포·삭제·권한 변경처럼 되돌리기 어려운 일인가? 백업·승인·롤백 기준을 먼저 확인한다.
4. 실행 뒤 무엇을 기록해야 하는가? 매뉴얼의 `기록`과 `완료 조건`을 채운다.

## ID 읽는 법

`COK-GOV-03-07`은 `운영 원칙·의사결정` 도메인의 3번째 업무 흐름, 7번째 상황이다. 모든 파일은 같은 형식이라 검색 도구 없이도 읽을 수 있다.

## 재생성

`python3 scripts/generate_manual_system.py`를 실행하면 이 폴더의 생성 파일을 다시 만들 수 있다. 사람이 직접 수정할 내용은 `00-core-playbooks.md` 또는 프로젝트의 실제 가이드 문서에 기록하고, 생성 파일에는 직접 편집하지 않는다.
"""


def safe_name(text: str) -> str:
    return text.replace("·", "-").replace(" ", "-").replace("/", "-")


def manual_text(code: str, domain: str, role: str, domain_purpose: str, workflow: str, context: str, workflow_index: int, context_index: int) -> str:
    return f"""---
id: {code}
domain: {domain}
role: {role}
workflow: {workflow}
context: {context}
classification: generated-checklist
---

# {workflow}: {context}

## 목적

{domain_purpose}

## 역할

**{role}**가 실행한다. 답변 원문·개인키·복구 비밀번호가 필요한 경우에는 직접 열람하거나 전송받지 않고, 소유자가 자신의 기기에서 확인하도록 안내한다.

## 시작 조건

- `{context}` 상황에서 `{workflow}`가 필요해졌다.
- 이 작업의 사용자 영향, 개인정보 가능성, 되돌릴 수 있는지 여부를 먼저 확인했다.
- 새 기능 개발 요청이면 기능 동결·검증 기준을 확인했다.

## 실행 절차

1. **범위를 적는다.** 무엇을 바꾸거나 확인하는지, 대상 폼·화면·기간을 한 문장으로 기록한다.
2. **민감도를 분류한다.** 답변 원문, 이메일, 개인키, 복구수단, 접근 권한이 포함되면 해당 값을 복사·전송하지 않는다.
3. **현재 상태를 확인한다.** 운영 화면, 저장소의 최신 커밋, 관련 설정 또는 기록을 읽고 추측으로 실행하지 않는다.
4. **가장 작은 안전한 조치를 한다.** 변경은 한 번에 하나만 적용하고, 되돌릴 방법을 확인한다.
5. **영향을 확인한다.** 사용자 핵심 경로 또는 해당 운영 지표가 예상대로 동작하는지 점검한다.
6. **기록한다.** 아래 기록 항목과 완료 조건을 채우고, 예상과 다르면 다음 단계로 넘어가지 않는다.

## 판단 기준

| 상황 | 행동 |
|---|---|
| 답변 원문·개인키·복구 비밀번호가 필요해 보임 | 중단하고 `SEC` 또는 `SUP` 매뉴얼로 이동 |
| 배포·삭제·권한 변경이 필요한데 롤백이 없음 | 중단하고 `OPS` 또는 `DAT` 매뉴얼로 이동 |
| 새 기능 요청인데 사용자 증거가 없음 | 구현하지 말고 `PRD` 가설 기록으로 이동 |
| 개인정보가 포함될 수 있는 테스트·지원 요청 | 더미 데이터로 전환하고 `LEG` 또는 `SAL` 매뉴얼로 이동 |
| 장애·보안 의심 | 새 변경을 멈추고 `BCP` 매뉴얼로 이동 |

## 금지 사항

- 개인키, 복구 비밀번호, 키 백업, 응답 원문을 채팅·이슈·분석 도구·문서에 넣지 않는다.
- 운영 장애 중 원인을 모른 채 데이터베이스·암호문·권한을 직접 수정하지 않는다.
- ‘한 사람 1회’를 보장할 수 없는 브라우저 제한을 채용·선별·투표의 공정성 장치로 표현하지 않는다.
- 구현·배포만 끝난 기능을 사용자 검증 완료로 표시하지 않는다.

## 기록

- 작업 ID: `{code}`
- 실행 일시와 실행자
- 대상과 변경 범위
- 개인정보·보안 영향: 없음 / 가능성 있음 / 확인 필요
- 확인한 결과와 남은 위험
- 다음 검토 날짜

## 완료 조건

- 핵심 사용자 경로 또는 운영 확인 항목을 실제로 점검했다.
- 원래 의도하지 않은 개인정보·키·응답 원문이 기록되지 않았다.
- 실패 시 되돌릴 위치 또는 후속 담당자가 문서에 남아 있다.
- 필요하다면 도메인별 실제 가이드와 연결했다.

## 다음 연결

- 도메인 README의 `{domain}` 섹션을 확인한다.
- ID `{code}`의 결과가 새 가설을 만들었다면 `PRD` 도메인에 기록한다.
"""


def run() -> None:
    if OUT.exists():
        shutil.rmtree(OUT)
    manuals_root = OUT / "manuals"
    manuals_root.mkdir(parents=True)
    (OUT / "README.md").write_text(README, encoding="utf-8")
    (OUT / "00-core-playbooks.md").write_text(CORE_PLAYBOOKS, encoding="utf-8")

    rows = ["id,domain_code,domain,role,workflow,context,path"]
    domain_lines = ["# 도메인별 책임과 기존 문서 연결", "", "| 코드 | 도메인 | 주 역할 | 기존 우선 문서 |", "|---|---|---|---|"]
    count = 0

    existing = {
        "SEC": "`docs/security/e2ee-and-key-management.md`",
        "LEG": "`docs/guides/privacy-operations.md`, `docs/legal/`",
        "SUP": "`docs/guides/troubleshooting.md`",
        "DAT": "`docs/guides/responses-and-exports.md`",
        "SAL": "`docs/validation-freeze-and-readiness-2026-08.md`",
        "GOV": "`docs/validation-freeze-and-readiness-2026-08.md`",
        "COM": "`docs/cokform-korean-brand-growth-and-product-plan-2026-08.md`",
    }

    for domain_code, domain, role, purpose, workflows in DOMAINS:
        domain_dir = manuals_root / domain_code.lower()
        domain_dir.mkdir()
        domain_lines.append(f"| {domain_code} | {domain} | {role} | {existing.get(domain_code, '이 매뉴얼 시스템의 핵심 플레이북')} |")
        domain_index = [f"# {domain_code} · {domain}", "", f"**주 역할:** {role}", "", f"**책임:** {purpose}", "", "## 업무 흐름", ""]
        for workflow_index, workflow in enumerate(workflows, start=1):
            domain_index.append(f"### {workflow_index:02d}. {workflow}")
            domain_index.append("")
            for context_index, context in enumerate(CONTEXTS, start=1):
                code = f"COK-{domain_code}-{workflow_index:02d}-{context_index:02d}"
                filename = f"{code}.md"
                relative = f"manuals/{domain_code.lower()}/{filename}"
                (domain_dir / filename).write_text(
                    manual_text(code, domain, role, purpose, workflow, context, workflow_index, context_index),
                    encoding="utf-8",
                )
                domain_index.append(f"- [{context}](./{filename})")
                rows.append(f'"{code}","{domain_code}","{domain}","{role}","{workflow}","{context}","{relative}"')
                count += 1
            domain_index.append("")
        (OUT / f"{domain_code}-README.md").write_text("\n".join(domain_index) + "\n", encoding="utf-8")

    (OUT / "DOMAIN-README.md").write_text("\n".join(domain_lines) + "\n", encoding="utf-8")
    (OUT / "INDEX.csv").write_text("\n".join(rows) + "\n", encoding="utf-8")
    (OUT / "MANIFEST.txt").write_text(f"Generated manual count: {count}\nExpected manual count: 2000\n", encoding="utf-8")
    if count != 2000:
        raise RuntimeError(f"Expected 2000 manuals, generated {count}")
    print(f"Generated {count} manuals at {OUT}")


if __name__ == "__main__":
    run()
