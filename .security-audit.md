# Supabase security audit — 2026-08-17

Read-only query against project `bnbdxcyarerrmijuvxdb`:

```sql
select n.nspname as schema_name, c.relname as table_name, c.relrowsecurity as rls_enabled
from pg_class c
join pg_namespace n on n.oid = c.relnamespace
where n.nspname = 'public'
  and c.relname in ('forms', 'form_public', 'responses')
order by c.relname;
```

Result: `0 rows`, `Success. No rows returned`.

Conclusion: the Cokform production tables are not currently present in the Supabase project, so the application is not yet using the intended forms/form_public/responses RLS schema. Apply `supabase/schema.sql`, then rerun the policy and privilege audit before enabling the pilot.


After running the full schema script in the Supabase SQL Editor, a fresh RLS table query still returned `0 rows`. The SQL Editor displayed `Success. No rows returned` for the DDL, but the target tables were not visible in the follow-up query. Do not treat the schema as active yet; verify the SQL Editor database/branch and rerun a minimal table-creation/query check.
